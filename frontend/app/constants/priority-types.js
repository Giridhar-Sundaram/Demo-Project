export const PRIORITY_TYPES = Object.freeze({
  LOWEST: 'lowest',
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
  HIGHEST: 'highest',
});

export const PRIORITY_VALUES = Object.freeze(Object.values(PRIORITY_TYPES));
